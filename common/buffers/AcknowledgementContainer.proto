package its;

import "TrajectoryPoint.proto";

message AcknowledgementContainer {
	required uint64 targetStationID = 1;
	required int64 adviceAccepted = 2;
	repeated its.TrajectoryPoint followedTrajectory = 3;
}